import { ExchangeLockerModel } from './exchange-locker.model';

describe('ExchangeLockerModel', () => {
  it('should create an instance', () => {
    expect(new ExchangeLockerModel()).toBeTruthy();
  });
});
